import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.nancyhenry.ma3ankolyoum",
  appName: 'ma3an-kol-youm',
  webDir: 'dist'
};

export default config;
